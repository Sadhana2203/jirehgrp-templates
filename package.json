{
  "scripts": {
    "test:templates": "node scripts/test-templates.mjs",
    "typecheck:safe": "node scripts/safe-typecheck.mjs",
    "build:safe": "node scripts/safe-build.mjs",
    "release": "changeset version && git add . && git commit -m \"chore(release): version bump\" && git push && git push --tags"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "npm-check-updates": "^18.0.2"
  }
}
